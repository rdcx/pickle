version: "3.9"
services:
  greeter:
    build: ./greeter
    
  person.show:
    build: ./person.show
    depends_on:
    - redis
    
  person.store:
    build: ./person.store
    depends_on:
    - redis
    
  gateway:
    build: ./gateway
    ports:
      - "8080:80"
  redis:
    image: "redis:7-alpine"
